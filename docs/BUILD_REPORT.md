# BUILD REPORT

## Work Order

R-0087 / R-0088 / R-0089 / R-0090 — Jonathan's 05A review rulings, executed as one revision set (Sprint 05A remains six orders; these amend four of them)

## Version

v0.1

## Summary

- THE ARRIVAL (R-0089, docs/progress/R-0089-arrival.mp4 + R-0089.png +
  R-0089-approach-frames.png): the opening now begins ON the full
  composition the room was built for (the R-0071 standing frame, held
  for a beat), walks to the desk, and settles a chair's depth back with
  a LEVEL regard — what a seated worker actually has in their line of
  sight: the wall in front of them (the hero print, the notes, the
  studio's letterpress note) with the desk in the frame's lower third,
  eyes 28cm above the work surface. No downward stare at rest; looking
  down is what choosing JOURNAL or CONTACT does. Measured from the
  recording: opening hold to full settle ≈ 3.5s — inside the 4-second
  criterion.
- THE HERO SHOT (R-0088): the moving image moved from the considered
  print to the large print above the pencils, exactly as ruled — and it
  begins playing a breath (0.6s) after the perspective settles, which
  lands the first unprompted movement at ~4.2s: the ~5-second moment,
  in plain sight. The print was re-hung a step lower (y 1.52 → 1.30)
  so the seated frame holds it whole — the maker re-hung it where it
  can be judged from the chair. Supersedes "the hero print stays
  blank." Verified on the LIVE site: the poster region shows max pixel
  change 126 over 3 seconds after settle. The considered print returned
  to plain paper.
- THE LOGO (R-0087, docs/progress/R-0087-logo-note.png): the HTML
  overlay is gone. The letterpress logo is now printed on the A5 note
  pinned above the lamp — on the note's own paper tone, curling off the
  wall, lit and shadowed with everything else. Part of the room, never
  an overlay. Fixing this exposed a latent bug: the curling note had
  tilted INTO the plaster since 0054 (two-thirds buried); the curl sign
  is corrected and the note re-pinned a hand lower for the seated frame.
- THE CHAIR (R-0089 staging): re-staged to Jonathan's ruling — angled
  toward the desk, moved to the side, the way a chair stands when its
  sitter pushed back, swiveled, and got up. Clearly read in the
  arrival's wide opening; behind the visitor's shoulder at rest.
- HEAD-ONLY TURNS (R-0090, docs/progress/R-0090-journal/films/contact):
  conversations no longer move the body — the camera position is
  pinned; choosing a destination turns the HEAD toward it, one ease,
  and turns back on release. The room-quiet and the journal's
  illumination ride the same ease. Captions were re-placed on measured
  clear wall for the new frames (the hero re-hang claimed the old
  spots); the journal's on-page words were rewritten larger (the maker
  writes big — the head never travels closer). Behavioral verification
  passes on the LIVE deploy: flyby/rest/release for all three, plus an
  idle-pointer guard (the framework's default pointer sits at frame
  center, which now rests on the prints — labels no longer self-appear
  before the visitor's first mouse movement).
- The review film was re-shot on the live site with the new grammar
  (docs/progress/R-0091.mp4).

## Decisions Required

Readability of the journal's page from the fixed seat: with the head
pinned to the chair and an A5 page a meter away, the words must be
written large to read at all — the current placeholder does, but the
authored voice may want more words than the page can carry at that
size. Options when 05B is authorized: accept large sparse handwriting,
surface the journal's text as a caption-register overlay in addition to
the page, or allow JOURNAL a one-time closer regard. Jonathan's call.

## Ready for

Jonathan's re-review of 05A under his amendments, and the 05B strategy
decision (recommendation delivered in-session).
