# BUILD REPORT

## Work Order

WORK ORDER 0013 — Establish the Window

## Version

v0.1

## Summary

- The window is now the daylight's believable architectural source. Three changes made it work: the frosted pane uses an unlit material (the window wall is backlit, so a lit material rendered the glass as a dark hole — the glass must BE the light); the opening gained a 10cm reveal with sill, head, and jamb returns (the minimum trim for believable construction); and the window shifted slightly rearward so a quiet sliver enters the locked frame's right edge.
- The sun was not moved; the lighting is unchanged — it is now explained. The window wall correctly reads as the darkest wall (backlit), with the bright glass in it.
- IMPORTANT DISCLOSURE — capture fidelity: progress screenshots 0009 through 0012 were horizontally stretched about 1.4x by a viewport-aspect bug in the capture pipeline (the capturing browser's non-16:9 viewport was squeezed into 1280x720). 0013.png is the first true-to-proportion frame since 0008. The locked opening composition was therefore reviewed in distorted form; the true frame shows the bench correctly proportioned with more surrounding space. The pipeline is fixed permanently: capture runs render in a pinned 1280x720 canvas.
- CHANGELOG updated: the daylight found its window.

## Decisions Required

Please re-review the opening composition in the true-proportion frame (0013.png). The Opening Composition lock was approved against horizontally stretched captures; it may stand as-is, or you may wish to issue a revision now that the frame is faithful.

## Ready For

Creative review of docs/progress/0013.png, then WORK ORDER 0014.
